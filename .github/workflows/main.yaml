name: Build, Push and Deploy

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_HANO4578 }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: ./dockers/Image
          file: ./dockers/Image/dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_HANO4578 }}/my-ml-app:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GCP VM via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCP_VM_HOST }}
          username: ${{ secrets.GCP_VM_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          port: 22

          script: |
            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_HANO4578 }}" --password-stdin

            docker stop my-app-container || true
            docker rm my-app-container || true

            docker pull ${{ secrets.DOCKERHUB_HANO4578 }}/my-ml-app:latest

            docker run -d \
              --name my-app-container \
              -p 80:8000 \
              ${{ secrets.DOCKERHUB_HANO4578 }}/my-ml-app:latest

            docker image prune -f